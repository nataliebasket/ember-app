<div class="container h-100">
		<div class="htop">
			<h2 class="text-center">Редактирование спикера: {{model.first_name}} {{model.last_name}}</h2>
      {{speaker-form speaker=model onsubmit=(action "saveSpeaker") onclick=(action "cancelSaveSpeaker")}}
		</div>
	</div>